{
  "name": "EPIC MAIL",
  "description": "A system that allows efficient, cost-effective mail sending operations",
  "env": {
    "APP_URL": {
      "required": true
    },
    "NODE_ENV": {
      "required": true
    }
  }
}